[project]
name = "focusguard"
version = "0.1.0"
description = ""
authors = [
    {name = "Belov Alexey",email = "lcf-trane@live.ru"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python (>=5.0.0.93,<6.0.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "black (>=26.5.1,<27.0.0)"
]
